# Dependencies & build output
node_modules/
build/
dist/
coverage/

# OS / logs
.DS_Store
*.log

# Environment & secrets (keep the example)
.env*
!.env.example
*.pem
*.key
*service-account*.json
*.credentials.json

# Firebase CLI artifacts (if you use the CLI to deploy rules)
.firebase/
firebase-debug.log
.firebaserc

# Packaged uploads
*.zip

# Tooling / local notes
.claude/
AI_STUDIO_PROMPTS.md
